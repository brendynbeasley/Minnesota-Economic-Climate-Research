# Archive

This folder holds older root-level and helper files that were moved out of the main project tree during cleanup.

- `root_legacy/`: duplicate or one-off files that were previously sitting at the project root
- `code_legacy/`: trivial helper scripts kept only for recordkeeping

These files are preserved for provenance, but they are not the preferred entry points for the project.
